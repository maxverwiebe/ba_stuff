# Sources for my bachelor thesis

`code/` contains most of the Python / C++ code.
`thesis/Clustering Methods for Optimizing Index Partitioning/` contains the Latex sources of the thesis document.

## Code Documentation

`run_code.md` contains a small code documentation.

## Build Latex Document

To build the Latex document with pdflatex and biber call the script `thesis/Clustering Methods for Optimizing Index Partitioning/compile_full.sh`.
